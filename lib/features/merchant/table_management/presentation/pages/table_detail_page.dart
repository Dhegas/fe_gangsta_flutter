import 'package:flutter/material.dart';

class TableDetailPage extends StatelessWidget {
  const TableDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Table Detail')),
      body: const Center(
        child: Text('Detail meja akan diimplementasikan pada iterasi berikutnya.'),
      ),
    );
  }
}
